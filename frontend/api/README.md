This folder is intended to hold `app/api` route handlers or other backend-facing helpers for the frontend app.

Use Next.js `app` router API routes (for example: `app/api/objects/route.ts`) and keep server-only helpers here.
